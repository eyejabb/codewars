function century(year) {
    // Finish this :)
    //take parameter of 'year'
    //round 'year' up to the nearest integer
    //split 'year' to get first two numbers from first two indexes
    //join numbers to get result
    return Math.ceil(year / 100);
  }